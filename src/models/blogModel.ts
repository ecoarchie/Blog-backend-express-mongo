export type BlogViewModel = {
  id: string;
  name: string;
  description: string;
  websiteUrl: string;
  createdAt: string;
};

export type BlogInputModel = {
  name: string;
  description: string;
  websiteUrl: string;
};
